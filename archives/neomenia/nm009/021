->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
  ú  ú  ú  ú  ú  ú  ú  ú  ú  ú  ú  ú  ú  ú  ú  ú  ú  ú ú 
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-

  021. MP3 en la red: Napser....................................por Skorpion

->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
 ú  ú  ú  ú  ú  ú  ú  ú  ú  ú  ú  ú  ú  ú  ú  ú  ú  ú ú  ú
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-

        Desde su aparici¢n, el formato MP3 ha producido una revoluci¢n en
  el mundo de la m£sica digital, llegando a ser el segundo tema m s buscado
  despu‚s del sexo (supongo que este siempre ser  el primero ;) ). Pero
  vamos con lo que nos interesa: el programa Napser.

        Napser (disponible en http://www.napser.com) es un programa de
  reciente creaci¢n (es de este mismo a¤o) que ya va por la segunda versi¢n
  en el programa cliente.

        Pero vamos por partes: La idea proviene de los canales sobre MP3
  del IRC. De hecho el programa Napser es un cliente que se conecta a un
  servidor (solo el server de napser.com) y nos muestra la lista de canales
  creados (los canales son fijos y no se pueden crear). Una vez conectados,
  veremos las diferentes opciones disponibles: "Chat", "Library", "Search",
  "Hot List" y "Transfer".
        
        La opci¢n de "Chat" es muy simple, y sirve para entrar o salir de
  los canales, mostr ndonos a los usuarios conectados a cada canal, junto
  con el n£mero de MP3 que dispone cada uno, el estado de su conexi¢n y el
  tipo de conexi¢n que posee (cable, m¢dem, etc.). Si tenemos en cuenta que
  hay unos 1.500 clientes registrados, suele haber unos 700 Gb. de MP3...
  Una cifra a considerar.
        
        Nosotros tambi‚n podemos poner a disposici¢n de los dem s los MP3
  que queramos, pudiendo ser 0, aunque de esta forma se perder¡a la idea de
  comunidad en la que se basa el programa (que es totalmente freeware, por
  cierto).
       
        Solo muestra las canciones de las personas que est n conectadas, y
  permite la b£squeda tanto por autor como por tema, ofreci‚ndonos la opci¢n
  de "Resume".
   
        En los canales no se habla absolutamente de nada, yento toda la gente
  a por los MP3 a saco. Tiene una tasa de transferencia bastante buena (a
  m¡, con un m¢dem normalito sobre 4'5 Kb. por segundo) y el "£nico problema"
  es que hay muchas canciones repetidas, aparte de poder encontrar m£sica
  alternativa a las Spice Girls o Britney Spears. Pero bueno, esto es como
  la radio: buscando, buscando...
        
        Por el momento, el programa solo rula bajo Windoze, aunque espero
  que con el tiempo lo pasen a otras plataformas. Tambi‚n decir que he leido
  que van a denunciar al programa por lo de siempre (Ya sab‚is como est n
  las discogr ficas al ver como se les va la pasta. Ah¡ es donde m s les
  duele).
        
        Para terminar con este art¡culo, remcomendaros una p gina Web intere-
  sante sobre MP3 de rock (tanto nacional como guiri), tambi‚n con skins:
  http://perso.wanadoo.es/katxorro

        Un saludo a todos los lectores.
