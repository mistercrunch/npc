---
title: "nm001"
ezine: "neomenia"
---

# nm001

**Ezine:** neomenia

<div className="ascii-content">

```


            ██   ██ ████ ██████ ██   ██ ████ ██   ██ ██    ███
            ███  ██ ██   ██  ██ ███ ███ ██   ███  ██      ██ ██
            ██ █ ██ ███  ██  ██ ██ █ ██ ███  ██ █ ██ ██   ██ ██
            ██  ███ ██   ██  ██ ██   ██ ██   ██  ███ ██  ███████
            ██   ██ ████ ██████ ██   ██ ████ ██   ██ ██  ██   ██
            -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-= 
            · · · · · · · · E-ZINE UNDERGROUND · · · · · · · ·
            =-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-

 %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
 %                                                                          %
 %                              ¡¡¡ATENCIαN!!!                              %
 %                                                                          %
 %  Los creadores y colaboradores de esta publicación NO se hacen responsa- %
 % bles, bajo ningún concepto del mal uso de la información aquí expuesta   %
 % por parte del lector/a. No se admiten reclamaciones.                     %
 %                                                                          %
 %  Si no aceptas y entiendes en su totalidad este termino, no debes seguir %
 % leyendo Neomenia.                                                        %
 %                                                                          %
 %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

                 Nombre: Neomenia

                 Número: 1

                 Fecha: 13 de junio de 1.999

                 Planeta: Tierra (Sistema Solar)

                 Visualización: Edit del DOS

                 Director: Dragomàn
                 Redactores: Kapitán Sponja y Dragomàn

                 E-Mail: dragoman@softhome.net

                 Noticias: Amatoma Press (amatoma@softhome.net)

                 Mailing List: Under 2000 (under2000-subscribe@egroups.com)



☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺
· · · · · · · · · · · · · · · · · · INDICE · · · · · · · · · · · · · · · · · ·
· · · · · · · · · · · · · · · · DE CONTENIDOS· · · · · · · · · · · · · · · · ·
☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻☺☻


        1. Presentación....................................Neomenia Group
        2. Noticias........................................Amatoma Press
        3. Escaneo de líneas telefónicas...................Dragomàn
        4. Comenzando con Linux............................Dragomàn
        5. Curso de programación en C......................Kapitán Sponja
        6. El virus CIH o Chernobyl........................Dragomàn
        7. Nueva lista de correo electrónico: Under 2000...Neomenia Group
        8. Despedida.......................................Neomenia Group



->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-
 1. Presentación..............................................................
    .......................................................por Neomenia Group
->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-


        Empieza el verano y empieza el nuevo e-zine Neomenia. Bienvenidos y
  bienvenidas a esta primera edición. Esto es un e-zine (o un fanzine elec-
  trónico, como queráis llamarlo) dedicado al mundo underground. 

        El único motivo por el que hemos creado esta publicación, es el inte-
  rés en intercambiar información y conocimientos con todo aquel interesado.

        Aquí no conocemos la censura. Esta publicación es totalmente libre y
  está abierta a todo tipo de colaboraciones. Si sientes interés en intercam-
  biar tus conocimientos con el resto de la sociedad internauta, puedes cola-
  borar enviando un e-mail a dragoman@softhome.net.

        En esta primera edición tendremos un poco de información sobre el
  virus Chernobyl y el war-dialing, un nuevo curso de programación en C que
  dirigirá el Kapitán Sponja y, naturalmente las noticias más fresquitas de
  Amatoma Press.

        Que lo disfrutes!



->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-
 2. Noticias..................................................................
    ........................................................por Amatoma Press
->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-


        La información ofrecida en el siguiente apartado, es ofrecida por
  Amatoma Press, un servicio que ha colaborado con numerosas páginas Web y
  con el e-zine Selva Digital hasta su número 6, y que a partir de ahora va
  a pasar a formar parte al 100% de Neomenia.

        Si lo deseáis, podéis recibir las últimas noticias por correo electró-
  nico. Para ello, simplemente tenéis que apuntaros a la lista de correo
  electrónico Under 2000, enviando un mensaje vacio, sin cuerpo ni subject a
  under2000-subscribe@egroups.com.


   ------------------------------------------------------------------------
   J.J.F. Hackers organiza una Con en Mallorca-----------------------------
   ------------------------------------------------------------------------

   El grupo  J.J.F. Hackers Team, ha organizado  una Con  que tendrá  lugar
   este mismo mes  en Mallorca. Puedes  reservar entradas  hasta el día 10.
   Si quieres  más información o comprar entradas (solo vale 2.000 pesetas)
   visita la Web de J.J.F. en http://www.jjf.org.


   ------------------------------------------------------------------------
   La hora de JazzTel y BT-------------------------------------------------
   ------------------------------------------------------------------------

   Ya  podemos  contratar  servicios telefónicos  con  dos nuevas compañías
   telefónicas:  JazzTel  y  British Telecom.  Ambas o frecen  tarifas  muy
   competitivas (sobretodo JazzTel), y en breve ofrecerán acceso a Internet
   gratis para sus clientes.


   ------------------------------------------------------------------------
   Llega el K7-------------------------------------------------------------
   ------------------------------------------------------------------------

   El nuevo procesador de AMD, el K7, ya está muy  cerca, y con unas carac-
   terísticas  iguales  o  superiores  a  las  del Pentium  III  de  Intel.


   ------------------------------------------------------------------------
   ONO: Llegó la fibra óptica----------------------------------------------
   ------------------------------------------------------------------------

   ONO va a  ser la primera  compañía en  nuestro país, que  va a empezar a
   trabajar y a ofrecer servicios con redes de fibra  óptica, substituyendo
   así a las  líneas de cobre  propiedad de  Telefónica. Más información en
   el 900 111 020.


   ------------------------------------------------------------------------
   Bleem! ya está disponible-----------------------------------------------
   ------------------------------------------------------------------------

   Desde hace unas semanas, ya está disponible la versión final de Bleem!,
   el emulador oficial de la consola Play Station.


   ------------------------------------------------------------------------
   Dolphin: La bestia de Nintendo------------------------------------------
   ------------------------------------------------------------------------

   Con el nombre de Dolphin  o Nintendo 2000, se conoce el próximo proyecto
   de Nintendo.  Una nueva consola  de 128 o  256  bits, que  probablemente
   funcione con discos DVD.


   ------------------------------------------------------------------------
   Internet desde la consola-----------------------------------------------
   ------------------------------------------------------------------------

   Dentro  de  poco tiempo,  podremos comprar un nuevo  periférico para las
   consolas Nintendo 64 y Play Station, que nos permitirá hacer partidas en
   red y conectar a Internet desde el televisor, al más puro estilo WebTV.



->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-
 3. Escaneo de líneas telefónicas.............................................
    .............................................................por Dragomàn
->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-



        En el siguiente artículo, os voy a hablar de sobre la actividad de
  escanear líneas telefónicas, también conocida con el nombre de dialing o
  war-dialing (originario de la película War Games -Juegos de Guerra-).

        El war-dialing, consiste en llamar uno por uno a todos los números
  de teléfono de una ciudad, o una zona, para averiguar cuales tienen fax,
  contestador automático, cuales están conectadas a una centralita... o lo
  más interesante de todo: cuales disponen de módem.

        Pero claro, llamar uno por uno a todos los números de teléfono, sería
  realmente pesado. Tardaríamos mucho y no lograríamos muy buenos resultados.
  Para llevar a cabo esta actividad, existen programas llamados war-dialers
  o discadores, que de forma automática, llamarán a una serie de números de
  teléfono que le indiquemos.

        Así, si por ejemplo queremos llamar desde el teléfono 91 123 00 00 al
  91 123 99 99, solo tendremos que indicarle al programa estos dos teléfonos,
  y él ya se encargará de llamar a todos. Nada más ni nada menos que a 1.000
  números sin que tengamos que marcar nosotros.

        Existen discadores que con tan solo unos segundos son capaces de pro-
  porcionarnos la información necesaria sobre las líneas. Esto es muy impor-
  tante, porque tan solo con que descuelguen el auricular ya tendremos que
  pagar el establecimiento de la llamada (a no ser que llamemos por UNI2, por
  JazzTel o por British Telcom...).

        Otro problema importante, es si la compañía Telefónica llega a sospe-
  char. Esto tenemos que evitarlo sea como sea, porque si sospechan de noso-
  tros no dejarán de vigilarnos.

        Si la operadora por la que llamemos observa que en muy poco tiempo
  hemos llamado a 1.000 números de teléfono de una misma ciudad, y que encima
  ha sido por orden numérico, sospechará. ¿Como evitarlo? Prestad atención a
  los siguientes consejos:

        -Es recomendable llamar a altas horas de la madrugada. Sobre las 03:00
         o 04:00 es una buena hora.

        -Ir cambiando de operadora telefónica. Si tenemos contradados varias
         operadoras como UNI2, Telefónica, Retevisión, BT, JazzTel, etc., es
         muy recomendable que vayamos cambiando de una a otra, introduciendo
         el prefijo correspondiente, para que no cante tanto.

        -No estar mucho rato escaneando. Unos minutos a la semana. A ser posi-
         ble tenemos que ir alternando varios días.

        Si tenéis un ordenador portátil, con una tarjega SCSI para poder
  conectar un teléfono móvil, hacédlo desde allí. Es muy dificil que os pillen
  de esta forma, siempre que se trate de un teléfono móvil sin contrato, como
  puede ser un MoviStar con una tarjeta Activa, un Airtel con una tarjeta
  Formula, un MoviLine con una tarjeta αptima o un Amena con una tarjeta Más.

        ¿Es legal escanear líneas? Pues... es algo complicado. No nos meterían
  en la cárcel, pero nos tendrian vigilados. Además, al estar escaneando las
  líneas, estas obteniendo información de las mismas, por lo que, aunque
  parezca mentira, podría considerarse un delito.

        Espero que tengáis exito con vuestro dialing, y no olvidéis tomar
  precauciones, cuantas más mejor!



->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-
 4. Comenzando con Linux......................................................
    .............................................................por Dragomàn
->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-


        Sé que hay muchas personas (demasiadas) que no conocéis lo que es
  Linux. O tal vez lo conozcáis pero no os atrevéis a instalarlo en vuestro
  ordenador... Este artículo, va dedicado para todas estas personas.


        ¿QUE ES LINUX?
        ~~~~~~~~~~~~~~
        Linux es un sistema operativo, derivado de Unix y totalmente gratuito,
  que creó un genio llamado Linus (de ahí viene su nombre) hace ya algunos
  años, y que gracias a su calidad y a su filosofía, es una excelente alter-
  nativa a otros sistemas como Windows, lo cual le hace ganar miles de usua-
  rios cada día en todo el mundo.


        ¿QUE TIENE QUE VER CON EL MUNDO UNDERGROUND?
        ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
        Mucho, pues al tratarse de un sistema operativo abierto de cara al
  usuario, modificable y tan relacionado con el mundo de las redes informáti-
  cas, nos facilitará enormemente las cosas y nos abrirá nuevos caminos en
  el mundo del hacking.


        ¿POR QUE DEBO INSTALARLO?
        ~~~~~~~~~~~~~~~~~~~~~~~~~
        Hombre, pues si te gusta la informática y sientes afición por el tema
  del hacking, deberías tener Linux instalado en tu disco duro. La cantidad
  de software freeware que posee o la cantidad de posibilidades que te ofrece,
  son dos razones fundamentales para que lo instales.


        A INSTALARLO!
        ~~~~~~~~~~~~~
        Bien, supongamos que te has convencido a instalar este genial sistema
  operativo, o ya conocías Linux con anterioridad pero no te atrevías a
  instalarlo en tu ordenador. Sistemas operativos como Windows, hacen que el
  usuario se acomode y se le atrofie el cerébro, por lo que a muchos de sus
  usuarios las palabras particionar el HD, generar disketes con las imágenes
  y otras cosas relacionadas con la instalación de Linux, hacen que se lo
  piense dos veces, y la mayoría de veces, se heche atrás. Pues bien, vamos
  con una guía rápida del proceso de instalación, ya que Linux es algo impres-
  cindible en el mundo del hacking, por lo que vamos a tratarlo mucho en esta
  publicación, así que más vale que lo aprendas si te gusta todo esto.

        A lo que íbamos. Vamos a instalarlo. Lo primero que nos hace falta es
  un CD-Rom con alguna de las abundantes distribuciones de Linux. En las
  actuales distribuciones, se ha ejemplificádo tanto el proceso de la instala-
  ción, que ya no es necesario tener que crear los discos con las imágenes
  ni nada.

        Existe una versión en castellano de Linux, llamada Eurielec Linux.
  Dicha versión se basa en el Red Hat 5.0, y es muy fácil de instalar.

        Bueno, supongamos que ya nos hemos hecho con algún CD de Linux, que
  hemos encontrado por alguna revista o que hemos comprado. Pues ahora a ins-
  talarlo toca.

        Si tenemos algún otro sistema operativo en nuestro ordenador, como
  por ejemplo el DOS, el Windows, el OS/2 o algún otro, tendremos que hacer
  una partición en el disco duro. Podemos hacer esto muy facilmente con la
  utilidad fdisk del MS-DOS. En este caso, es recomendable defragmentar los
  archivos del disco duro antes de proceder a la instalación.

        Si tenemos un disco duro formateado, que vamos a utilizar totalmente
  para Linux, el proceso se simplifica. Metemos el CD de Linux en la unidad
  lectora, y reiniciamos. Si nuestra BIOS arranca desde el CD, perfecto, y si
  no arranca no pasa nada. Metemos un disco de arranque, y nos vamos a la
  unidad del CD-Rom. Desde allí, aparecerá un ejecutable, normalmente llamado
  setup.exe, linux.exe, linux.bat, setup.bat, install.bat, install.exe...
  lo que sea, buscádlo y ejecutádlo.

        A partir de aquí empieza el sencillo proceso de instalación. Si no
  tienes mucha idea, y es la primera vez que lo instalas, no cambies la confi-
  guración que te ofrece el programa de instalación, pues normalmente es la
  más adecuada. Aceptamos siempre y le damos al "OK", metemos los nombres
  de usuarios y las contraseñas para ser root, y cuando acabe de copiar todos
  los archivos reiniciamos el ordenador.

        Una vez reiniciado, accedemos como root (no como col), y ejecutamos
  el programa X86setup para configurar la interfaz gráfica de X-Windows, y
  poder usar Linux como cualquier sistema operativo a modo gráfico.

        Ya está! Así de fácil ya tenemos instalado Linux en nuestro ordenador!

        A partir de ahora vamos a tratar profundamente a este sistema desde
  esta publicación.



->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-
 5. Curso de programación en C................................................
 ..........................................................por Kapitán Sponja
->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-



        1. INTRODUCCIαN:
        ~~~~~~~~~~~~~~~~
        Bueno, antes de comenzar con la materia me gustaría hacer unas aclara-
  ciones para que todos podamos aprovechar al máximo este curso.

        Antes de nada me presentaré: soy Kapitán Sponja y esta es mi primera
  incursión en cuanto a la escritura de artículos, por lo tanto espero que
  seáis buenos compañeros y me echéis una mano a mejorar. NO soy ningún gurú
  de la informática ni nada por el estilo, pero creo tener el nivel suficien-
  te para escribir esta sección de forma que todo el mundo pueda tener un
  acceso fácil al Lenguaje C, de forma que yo también repaso mis conocimien-
  tos.

        En esta sección no tengo previsto hacer una profunda revisión de todo
  el Lenguaje C, ni hacer virguerías ni nada por el estilo, simplemente inten-
  tar dar una primera aproximación a los novatos en esto de la programación,
  de cual es la filosofía del C y repasar un poco todos los puntos básicos que
  deben conocerse para poder hacer un programa en C.

        Bueno, muchos os preguntaréis: Pero, ¿por que C? Bueno, si estáis
  leyendo esta revista es que os interesa el mundo Underground, y supongo que
  ya habréis leído o álguien os habrá dicho que lo mejor para el hack es tener
  el sistema operativo Unix o Linux en su defecto, o por lo menos tener un
  amplio conocimiento de estos sistemas, ya que son los más utilizados en
  cuestión de redes. Si, bueno y eso, ¿que tiene que ver con el C? Calma,
  calma, Unix fue diseñado allá por los años 70 y, ¿adivináis en que lenguaje
  se programó? Si, C. TODOS los sistemas Unix llevan incorporado un compilador
  de C estándar, por lo tanto teniendo Unix y algo de conocimientos de C,
  podemos crear programas en C, compilarlos en la máquina que estemos y eje-
  cutárlos sin necesidad de traer el ejecutable ya compilado y arriesgarnos
  a que no funcione, o cualquier otro rollo que se os pueda ocurrir. Si os
  dáis una vuelta por otros e-zines, cosa que os recomeindo, no por que este
  sea malo, sinó porque todo lo que leáis y escuchéis serán más conocimientos
  sobre este interesante mundo que no deja de evolucionar, por lo tanto
  siempre puedes aprender muchas cosas de otra gente que sabe más que tu.
  Bueno, pues eso, que si miráis otros os daréis cuenta d ela cantidad de
  programas fuentes de C para Unix que ahy, ahí es donde voy, mi principal
  objetivo en esta sección es la de que cuando veas algno de estos programas
  no te limites a borrarlo por que no te interesa, o a usarlo sin ni siquiera
  entender una línea, sinó que seas capaz de seguir el curso del programa, y
  hasta modificarlo a tu gusto (si eres capaz, claro)

        Bueno, dicho esto comencemos un poquito con nuestro curso. Los que
  conozcáis algo de C bajo DOS/Windows, os recomiendo que leaís la sección
  de asignación de memoria, cuando lleguemos, ya que es la principal diferen-
  cia entre estos sistemas operativos, pues Unix gestiona la memoria de forma
  diferente al DOS. Los que no tengáis ni idea de C, seguídlo desde el prin-
  cipio y no os saltéis capítulos, ya que los capítulos siguientes se apoya-
  rán en los anteriores.


        1.1 COMPILADOR DE C:
        ~~~~~~~~~~~~~~~~~~~~
        El compilador que vamos a usar será el que trae por defecto de sistema
  operativo Unix, el CC o el GCC. Estos compiladores entienden un C estándar,
  pero... ¿que significa esto? Pues esto quiere decir que los programas en C
  que hagamos serán entendidos por cualquier compilador de C. Esto quita un
  poco de facilidad en algunos aspectos, por ejemplo no tendremos manejo de
  gráficos, ni de pantalla, ya que estas librerías no son estándares, pero así
  nos aseguramos de que nuestro programa funcionará en casa, en la Universidad
  o en un sistema que estamos hackeando. Si no tenéis Unix o Linux, también
  podéis seguir este cursillo, ya que lo daremos más a un nivel teórico que
  con muchos ejemplos (aunque siempre es bueno poder practicar, claro esta).

        Para compilar nuestros programas debemos escribir el código fuente
  con un editor cualquiera, y luego escribir desde el sistema operativo (GCC
  o CC, dependiendo del compilador que usemos):

  gcc nombre.c

        Esto genera un fichero llamado a.out donde tendremos nuestro ejecuta-
  ble, para dirigir el ejecutable a otro archivo usad la opción -o:

  gcc nombre.c -o nombre.x

        Para ejecutar el programa, escribid nombre.x


        1.2. PRIMEROS PASOS EN C:
        ~~~~~~~~~~~~~~~~~~~~~~~~~
        Pues sí, allá vamos. En este primer capítulo veremos el esquema gene-
  ral de un programa en C, para que vayáis tomando contacto con este lenguaje.

        Un programa en C se compone de tres partes principales:

        -Directivas al preprocesador y variables globales.
        -Función principal.
        -Zona de funciones.


        1.2.1. DIRECTIVAS AL PREPROCESADOR Y VARIABLES GLOBALES:
        ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~            
        Primero y antes de nada, definámos los terminos. Una directiva al
  preprocesador es una instrucción que se evalua antes de compilar el progra-
  ma. Hay distintas directivas, ya iremos vienod algunas de ellas, y para que
  sirven, por ahora saber que una directiva viene precedida de # por lo tanto
  si véis alguna instrucción en C con este simbolo ya sabéis lo que es.

        Variable global, es una variable que es conocida por todo el programa,
  es decir, tanto el principal como las funciones a las que llame. Dicho esto
  pasamos a definir esta zona, en un principio localizamos las directivas,
  posteriormente los prototipos de las funciones que creemos, que no he habla-
  do antes de ellas porque se incluyen en las directivas, y por último las
  variables globales. Esto es solo por estética y guardar un orden, si no
  queréis hacerlo así no estáis obligados.


        1.2.2. FUNCIαN PRINCIPAL:
        ~~~~~~~~~~~~~~~~~~~~~~~~~            
        Aquí tenemos el código de nuestro programa. En C, la función principal
  debe tener el nombre "main", si no es así nos dará un error. Tras main se
  abrirá una llave, y se escribirán las variables locales, que son las cono-
  cidas solo por la función donde son declaradas, y las sentencias del progra-
  ma. Al final se termina cerrando una llave.


        1.2.3. ZONA DE FUNCIONES:
        ~~~~~~~~~~~~~~~~~~~~~~~~~            
        En esta zona tendremos las funciones que hayámos creado para nuestro
  programa. Cada función debe tener su prototipo declarado en la zona de
  directivas, y posteriormente, debajo del main se desarrolla el código para
  cada función. Pero esto lo veremos con más detalle en próximos capítulos.


        1.3. PEQUEÑAS PARTICULARIDADES DE C:
        ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~          
        -Los comentarios en C se ponen de esta forma: /*comentario*/.
        -El Lenguaje C diferencia mayúsculas de minúsculas, por lo tanto será
         diferente la variable d1 de D1, mucho cuidado con esto.
        -Excepto las directivas, el resto de las sentencias en C terminan con
         un ; que no se nos olvide, o dará un error en la compilación.
        

        Para finalizar esta primera entrega, os doy este programita en C para
  que vayáis mirando lo aprendido y aclaréis algunas dudas:

/*Este es el programa más visto del mundo*/
/*COMIENZA LA ZONA DE DIRECTIVAS*/
#include "stdio.h"
#include "stdlib.h"
/*FIN DE LA ZONA DE DIRECTIVAS*/
/*COMIENZO DEL MAIN*/
main()
{
        system("clear")        
        printf("Esto funciona porque soy un monstruo.");
}
/*FIN DEL MAIN*/
/*AL NO HABER FUNCIONES NO HAY ZONA DE FUNCIONES Y EL PROGRAMA SE TERMINA Y
ESTA LISTO PARA SER COMPILADO*/


        Comentarios sobre el programa:

        Pasamos a ver línea a línea que hace este programa:

        Las dos primeras líneas, como podéis ver, son de comentarios, ya que
  vienen encerradas entre /* y */ como dije antes.

        Las siguientes dos líneas son directivas include. Este tipo de direc-
  tivas nos sirve para incluir un archivo dentro de nuestro programa, es como
  si añadieramos el código de este archivo al de nuestro programa. Por lo
  tanto, lo que hacemos es "añadir" el código de los archivos stdio.h y
  stdlib.h a nuestro programa. Y esto, ¿para qué? Pues muy simple, dentro de
  los archivos con extensión *.h, tenemos además de otras cosas los prototipos
  de las funciones que vamos a usar en nuestro programa. Esto quiere decir que
  por ejemplo, para poder usar la función printf(); que sirve para escribir
  por pantalla, debemos "decirle" al compilador como es esa función, o sea,
  que parametros recibe y que parametors retorna. Esto es lo que hace el
  prototipo de una función. En el fichero stdio.h, tenemos los prototipos de
  entrada y salida estandar (standar-input-output), es decir, salida por
  pantalla y entrada por teclado. El stdlib nos da el prototipo de system();
  que llama al sistema operativo para ejecutar un comando.
  
        Stdlib (standard library) es un conjunto de funciones de uso general.
  Existen más archivos de cabecera, como el math.h para operacioens matemáti-
  cas, pero stdio y stdlib nos serán indispensables casi siempre.

        Continuamos y encontramos a main(), esto nos indica que va a comezar
  el programa principal, los parentesis son necesarios aunque no pongáis nada,
  pues main puede recibir datos desde la línea de comandos, y estos irían ahí,
  pero por ahora limitaos a dejarlo vacío, ya que no lo necesitaremos aún.

        Abrimos la llave y comenzamos con el código. La sentencia system();
  llama al sistema operativo para ejecutar un comando, en este caso clear, que
  es similar al cls del DOS, para limpiar la pantalla, el comando a ejecutar
  debe ir entre comillas dobles ("").
        
        La siguiente sentencia es printf();. Esta sentencia es muy común ya
  que nos permite escribir en la pantalla. En este caso, escribimos lo que
  hay entre las comillas dobles. Más adelante estudiaremos más a fondo esta
  sentencia. Cerramos la llave y por lo tanto finalizamos el main, y con ello
  el programa.

        Pues bueno, con esto termina la primera entrega de este curso. Espero
  que no haya sido muy aburrido, ya que al ser el primero hay que explicar una
  serie de cosas poco amenas pero necesarias.

        Me despido ya. Si alguien quiere hacer cualquier tipo de crítica,
  comentario, tiene alguna duda o quiere información de cualquier tipo, puede
  escribir a: kapitansponja@olemail.com.

        Espero que con vuestra ayuda podamos mejorar esta sección y llegar a
  tener un buen manejo del lenguaje C.



->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-
 6. El virus CIH o Chernobyl..................................................
    .............................................................por Dragomàn
->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-


        El virus asiático CIH (cuyas iniciales se corresponden con el nombre
  de su propio autor, llamado Chen Ing-Hau), más conocido en su versión 1.2
  por la multitud con el nombre de virus Chernobyl, se ha convertido en el
  virus informático más peligroso, destructivo y temido por empresas y por
  particulares. ¿La causa? Se trata del primer virus que es capaz de dejar
  inútil nuestro equipo informático saltándose todas las protecciones y modi-
  ficando la BIOS.
        
        Por lo menos sus versiones actuales (1.2, 1.3 y 1.4) no son capaces de
  modificar BIOS que no sean flash, por lo que si nuestra BIOS no pertenece
  a este tipo, el virus no le hará efecto a nuestro ordenador.
          
        La versión 1.2, que como he dicho antes se denomina Chernobyl, se basa
  en el accidente de la central nuclear de Ucraina con el mismo nombre, que
  ocurrió el 26 de abril del año 1.986. Por eso esta versión y también la
  versión 1.3 se activan el día 26 de abril de cada año.

        Sin embargo, la versión 1.4 es capaz de activarse el día 26 de cual-
  quier més del año, por lo que resulta mucho más peligrosa.

        Ya se pueden contar por miles las personas de todo el planeta que se
  han visto obligadas a comprar una placa base nueva y remplazar la anterior
  para poder continuar utilizando su ordenador después de haber sido infecta-
  dos por este virus.

        Chernobyl reside en los huecos de memoria del código fuente de los
  archivios *.exe de Windows, lo cual le permite pasar desapercibido, ya que
  no aumenta el tamaño de dichos archivos y no hace sospechar al usuario de
  su existencia... Algo muy inteligente por su parte, ya que mucha gente cree
  que su disco duro está libre de virus simplemente por el hecho de que sus
  archivos no han aumentado de tamaño.
    
        El programador responsable de este diabólico virus informático, es un
  joven de nacionalidad taiwandesa, llamado Chen Ing-Hou (de ahí viene el
  nombre de su temida creación, que recibe las iniciales de su propio padre
  virtual).
        
        Pero antes de crear este virus, el joven Chen ya era famoso en su
  localidad por crear virus que infectaron los discos duros del centro de
  aprendizaje informático dónde adquirió todos sus conocimientos actuales
  sobre programación y el mundo vírico.
        
        ¿Qué será de él ahora que le han encontrado? Con lo fuertes que se
  pusieron con el creador del virus Melissa, ¿que le ocurrirá a este chico?
  El tiempo nos lo dirá...

        Aún no ha aparecido ningún antivirus comercial para capaz de combatir
  este virus y restaurar los archivos de un disco duro infectado por él, sin
  embargo, un estudiante llamado Monirul Islam ha creado una aplicación de
  libre distribución para luchar contra CIH. Se llama mRecover y la podréis
  encontrar de forma gratuita a través de Internet.

        Un aplauso para Monirul.



->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-
 7. Nueva lista de correo electrónico: Under 2000.............................
    .......................................................por Neomenia Group
->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-


        Under 2000 es una nueva lista de correo electrónico, totalmente abier-
  ta y libre, en la que podrás decir lo que quieras siempre y cuando sea bajo
  tu entera responsabilidad.

        El idioma principal es el español, pero en esta lista cabemos absolu-
  tamente TODOS.

        Hemos creado esta lista para que todos podáis intercambiar información
  sobre el mundo underground en general, pero también podéis hablar de otros
  temas, faltaría más.

        Que la lista esté abierta, no quiere decir que puedas intentar vender
  rosquillas o enviar un mensaje muchas veces. Si haces esto, lamentándolo
  mucho, te prohibiremos el acceso a la misma para siempre. Piénsalo.

        Si crees que puedes intercambiar información con los otros internautas
  que forman Under 2000, de una forma educada y civilizada, será un placer
  tenerte entre nosotros.

        Para apuntarte, solo tienes que enviar un e-mail en blanco a la sigui-
  ente dirección de correo electrónico: under2000-subscrie@egroups.com.

        Además, también recibirás las noticias de Amatoma Press en tu e-mail.



->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-
 8. Despedida.................................................................
    ........................................................por Neomenia Group
->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->->
♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦♦
<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-<-


        Pues aquí termina nuestro/vuestro primer número de Neomenia. Si os
  digo la verdad, no tengo ni idea de cuando podrá salir el número 2, pero no
  creo que tarde más de dos semanas.

        NO enviamos el e-zine por correo electrónico a nadie. Sed compresivos
  y tened en cuenta que no podriamos enviarlo a todos. El próximo número
  estará disponible en es.comp.hackers y en la lista de correo Under 2000.

        Si quieres convertirte en distribuidor oficial del e-zine, solo tienes
  que comunicárnoslo.

        Sin nada más que decir, nos despedimos. Hasta el próximo número!!!




```

</div>
