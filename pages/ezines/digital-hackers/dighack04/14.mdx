---
title: "14"
ezine: "digital-hackers"
---

# 14

**Ezine:** digital-hackers

<div className="ascii-content">

```
                      
                      xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
                      xx          Freepro          xx
   		      xx Mensajes anonimos en java xx 
                      xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
      
Hola soy CrashOverride con este codigo fuente hecho en java podran enviar
mensajes anonimos, lo unico que deben hacer es compilar el programa y mandarlo
a correr, y poner el nombre del servidor por ej. www.hotmail.??? .El codigo es
el siguiente:
 
import java.applet.*;
import java.awt.*;
import java.net.*;
import java.io.*;
import java.util.*;


public class Saeta extends Applet
{
	Image email,avi;
	Button enviar;
	Label l1, l2, l3;
	TextField direccion, sujeto;
	TextArea mensaje;
	String servidor;
	String usuario;
	int puerto = 25; // puerto SMTP


public boolean action (Event evt,Object Obj){
String calcula = (String)Obj;
	if(evt.target instanceof Button){
		if(calcula == "Enviar"){
		servidor = direccion.getText();
		usuario = sujeto.getText();
		int puerto = 25;   // puerto SMTP
	    try {
	 	// Abrimos un socket conectado al servidor y al
	 	// puerto del protocolo SMTP
	  	Socket socket = new Socket( servidor,puerto );
	    // Conseguimos el canal de entrada      
	 	BufferedReader entrada = new BufferedReader( 
		new InputStreamReader( socket.getInputStream() ) );
	 	// Conseguimos el canal de salida
	  	PrintWriter salida = new PrintWriter( 
		new OutputStreamWriter( socket.getOutputStream() 
),true );
	  
	  // Comenzamos la conversaci≤n con el servidor de
	  // correo electr≤nico
	 
	  salida.println( "mail from: "+usuario );
	  System.out.println( entrada.readLine() );
	  
	  salida.println( "rcpt to: "+usuario );
	  System.out.println( entrada.readLine() );
	  salida.println( "data" );
	  System.out.println( entrada.readLine() );
	  String timeStamp = (new Date()).toString();
	  salida.println(mensaje.getText()+timeStamp );
	  salida.println( "." );
	  System.out.println(entrada.readLine());
	  
	  // Cerramos el socket
	  socket.close();
	 } catch( UnknownHostException e ) {
	  e.printStackTrace();
	  System.out.println( 
	      "Debes estar conectado para que esto funcione bien." );
   	 } catch( IOException e ) {
	  e.printStackTrace();
	}
		}   	 
  }
	return false;
}
public String getAppletInfo() {
	return "Saeta\n" + 
		"\n" + 
}
public void init() {
	super.init();
	Color c= new Color(153,204,255);
	setBackground(c);
	l1 = new Label("Direccion: ");
	l2 = new Label("Sujeto: ");
	l3 = new Label("Mensaje: ");
	enviar = new Button("Enviar");
	direccion = new TextField(30);
	sujeto = new TextField(30);
	mensaje = new TextArea(30, 60);
	
	add(l1);
	l1.setBounds(5,5,10,10);
	
	add(direccion);
	direccion.setBounds(150,50,50,50);
	
	add(l2);
	l2.setBounds(5,20,20,20);
	
	add(sujeto);
	sujeto.setBounds(150,100,50,50);
	
	add(l3);
	l3.setBounds(5,50,20,20);
	
	add(mensaje);
	mensaje.setBounds(200,180,200,200);
	
	add(enviar);
	enviar.setBounds(500,700,20,20);
}
public void paint(Graphics g) {
	
} 
}

```

</div>
